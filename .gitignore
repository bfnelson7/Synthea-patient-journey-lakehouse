# Raw data — never commit the actual EHR CSVs (they're large + regenerable)
data/*.csv
output/
*.csv

# But DO keep a small sample so the repo is runnable
!data/sample/*.csv

# Databricks / system
.databricks/
__pycache__/
.DS_Store

# Notebook checkpoints
.ipynb_checkpoints/